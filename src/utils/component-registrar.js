/**
 * Global bileşen kaydı için yardımcı fonksiyon
 */
export function registerComponents(app) {
  // Burada genel bileşenler otomatik olarak kaydedilir
  // Örnek: Tüm UI bileşenleri otomatik kaydı
  
  try {
    // Global bileşenleri kaydet
    // Not: Bu kısım uygulamanın çalışması için kritik değildir
    console.log('Global bileşenler kaydediliyor...');
  } catch (error) {
    console.error('Bileşen kaydı sırasında hata:', error);
  }
} 